name: Deploy React Vite App to ECS

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USERNAME }}   # 应为 'deploy'
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            set -e

            echo "🔄 拉取最新代码..."
            cd /home/deploy/videotc
            git reset --hard
            git pull origin main

            echo "📦 安装依赖..."
            cd VTC-Web1.0
            npm install --production

            echo "🏗️ 构建 React 应用..."
            npm run build

            echo "📤 复制构建产物到 Web 目录..."
            sudo rsync -av dist/ /var/www/videotc/

            echo "✅ 部署成功！"
